{
  "mcpServers": {
    "proms-mcp-dev": {
      "url": "http://localhost:8000/mcp",
      "description": "Development mode - no authentication required"
    },
    "proms-mcp-prod": {
      "url": "https://proms-mcp.apps.cluster.example.com/mcp",
      "headers": {
        "Authorization": "Bearer ${OPENSHIFT_TOKEN}"
      },
      "description": "Production mode - requires OpenShift bearer token"
    }
  }
}
